{
  "name": "cyberpunk-invaders",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node app.js",
    "dev": "concurrently \"cd server && node index.js\" \"cd client && npm run dev\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "body-parser": "^2.3.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "negotiator": "^1.0.0"
  }
}
